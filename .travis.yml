language: java
jdk:
  - openjdk11
  - openjdk8
cache:
  directories:
    - $HOME/.m2
sudo: false
install: "mvn -Dmaven.javadoc.skip=true test"

after_success:
  - bash <(curl -s https://codecov.io/bash)

after_failure:
  - echo "build failed!"
